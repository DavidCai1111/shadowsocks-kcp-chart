apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shadowsocks-kcp-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "shadowsocks-kcp-chart.name" . }}
    helm.sh/chart: {{ include "shadowsocks-kcp-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "shadowsocks-kcp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
